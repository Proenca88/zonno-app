# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Node.js Portátil

Node.js NÃO está instalado no sistema. O executável portátil está em:
- **Caminho:** `D:\node-portable`
- Para usar em PowerShell: `$env:PATH = "D:\node-portable;D:\node-portable\node_modules\.bin;$env:PATH"`
- Versão: v22.11.0 / npm 10.9.0

# Regras Git

Após CADA alteração de código, fazer sempre:
1. `git add <ficheiros alterados>`
2. `git commit -m "mensagem descritiva"`
3. `git push origin main`

O repositório remoto é: `https://github.com/Proenca88/zonno-app.git`
Branch principal: `main`

**IMPORTANTE:** Usar sempre o PATH do node portátil antes de correr qualquer comando npm/npx.
